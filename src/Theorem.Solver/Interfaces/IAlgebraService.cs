namespace Theorem.Solver.Interfaces
{
    public interface IAlgebraService
    {
        decimal Somar(decimal operando1, decimal operando2);
        decimal Multiplicar(decimal operando1, decimal operando2);
    }
}
